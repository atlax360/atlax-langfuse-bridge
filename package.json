{
  "name": "atlax-langfuse-bridge",
  "version": "1.0.1",
  "description": "Torre de control FinOps para Claude Code en Atlax360 — hook Stop + reconciler cron + MCP server + Langfuse v3 self-hosted, cero deps prod.",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "https://github.com/Atlax-360-Test-IA/atlax-langfuse-bridge.git"
  },
  "keywords": [
    "claude-code",
    "langfuse",
    "finops",
    "observability",
    "bun",
    "mcp",
    "atlax360"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "tsc --noEmit && bun test"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.14",
    "typescript": "^5.9.3",
    "zod": "^4.4.3"
  }
}
